

```python

首先 克隆项目
  git clone https://github.com/fishermancome/flask_project_structure
其次 进入项目 安装依赖
  cd  flask_project_structure && pip3 install -r requirements.txt
接着启动项目
  python3 manage.py runserver 
打开浏览器 http://0.0.0.0:5000/api/index 即可看到页面输出
```
